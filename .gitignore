# Fichiers de Log
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Cache des paquets et dépendances locales
node_modules
.npm
.pnp.*

# Fichiers temporaires et de Diagnostic
*.tsbuildinfo
*.bak
*.swp
*.swo

# Fichiers d'environnement locaux (NE JAMAIS COMMITTER !)
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Répertoire de build TypeScript / Sortie Compilée
dist
out
build

# Cache et configuration spécifique à l'IDE
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Fichiers système macOS
.DS_Store
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Fichiers de couverture de tests
coverage
.nyc_output

# Fichiers générés par Sequelize-CLI (si vous préférez ne pas les committer,
# bien que config.json soit souvent commité avec des placeholders)
# config/config.json  <-- Décommentez si vous ne voulez PAS committer ce fichier

# Fichiers de base de données locale (ex: SQLite)
*.sqlite
*.sqlite3
*.db

# Fichiers d'upload locaux (si stockés dans le projet, ce qui N'EST PAS recommandé pour la production)
# Si votre dossier public/uploads est dans le repo mais que vous ne voulez pas committer
# les fichiers uploadés dynamiquement :
public/uploads/*
!public/uploads/.gitkeep # Garder le dossier s'il est vide avec un .gitkeep

# Fichiers spécifiques à certains outils ou OS
*.pem
*.cer

# Autres configurations locales
local.*
*.local